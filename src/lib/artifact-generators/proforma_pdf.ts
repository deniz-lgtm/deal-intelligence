import { notImplementedGenerator } from "./_stub";

// Phase 4: migrates /api/deals/[id]/proforma-pdf into this generator.
// The underlying src/lib/proforma-pdf.ts renderer stays — only the
// entry point (and the destination) change.
export default notImplementedGenerator("proforma_pdf");
