import { notImplementedGenerator } from "./_stub";

// Phase 4: new work. The LOI page today only uses window.print — a
// pdf-lib template needs to be authored here. Scoped in advance so it
// doesn't block the phase.
export default notImplementedGenerator("loi");
